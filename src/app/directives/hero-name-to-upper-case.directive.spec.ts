import { HeroNameToUpperCase } from './hero-name-to-upper-case.directive';

describe('HeroNameToUpperCase', () => {
  it('should create an instance', () => {
    const directive = new HeroNameToUpperCase();
    expect(directive).toBeTruthy();
  });
});
